export default function EmissionsFilters(props: Record<string, unknown>) {
  // Accept filters and onFiltersChange
  return <div className="text-white">Emissions filters (stub) {JSON.stringify(props || {})}</div>;
}
