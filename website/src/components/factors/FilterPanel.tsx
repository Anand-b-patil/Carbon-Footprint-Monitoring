export default function FilterPanel(props: Record<string, unknown>) {
  // Accept filters, onFiltersChange, onApply, onClear
  return <div className="text-white">Filter panel (stub) {JSON.stringify(props || {})}</div>;
}
